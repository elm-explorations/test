{
  "name": "elm-test-tests",
  "version": "0.0.0",
  "description": "tests for elm-test, so you can elm-test while you elm-test",
  "scripts": {
    "test": "rm -Rf elm-stuff && elm make --debug src/Main.elm --output elm.js && node elm.js"
  },
  "author": "Richard Feldman",
  "license": "BSD-3-Clause",
  "devDependencies": {}
}
