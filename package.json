{
  "name": "elm-test-tests",
  "version": "0.0.0",
  "description": "tests for elm-test, so you can elm-test while you elm-test",
  "scripts": {
    "test": "elm make && (cd tests && ./test.sh)"
  },
  "author": "Richard Feldman",
  "license": "BSD-3-Clause",
  "devDependencies": {}
}
